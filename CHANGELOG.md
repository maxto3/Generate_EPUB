# EPUB生成器更新日志

## [1.0.3] - 2026-09-12

### 变更

- GUI 迁移到 PySide6（Qt）：窗口与布局重写为 Qt Widgets；文本预处理移入后台线程（QThread），
  Pandoc 改用 QProcess 异步调用，新增进度条与状态栏反馈，转换期间界面保持响应。
- 合并策略（本地 × 远程 Qt 分支）：后台转换逻辑以本地实现为准，统一使用 `epub_preprocess.py`
  （编码探测、广告清洗、卷/章标题识别、统计），`generate_epub.py` 不再内联复制预处理逻辑。
- 保留本地增强：依赖自举（缺失时自动 pip 安装，含 PySide6/PyYAML/chardet）、文件名双模式解析
  （`《书名》…作者：` / `作者-书名`）、预处理完成后状态栏显示卷/章/简介/广告统计。
- 编译脚本改用 `--enable-plugin=pyside6`，移除 ttkbootstrap / tk-inter 相关参数。

## [1.0.2] - 2026-09-11

### 修复

- 修复卷/章标题误判：正文段落不再被错误标记为标题。典型症状如《诸界第一因》中被误判为
  Header 的「卷牍室比杨狱想象的要大，也更热闹。」「卷首的第一句话……」「卷宗上，字迹潦草……」
  「卷中记载，此人生卒年不详……」，全书同类误判共 6 处。
  原因：旧规则 `^(第.{1,2}卷|卷.{1,2}\s)` 用 `.{1,2}`（任意字符）充当卷序号，
  凡是以「卷」字开头的正文段落都可能命中；章规则 `^第.+章` 也是同样的宽泛写法。
- 新的标题识别规则要求「明确编号 + 分隔符」，并叠加长度与句号守卫：
  - 卷：`第X卷` / `卷X`（X 必须为数字），其后必须是行尾/空白/分隔符，且长度 ≤ 40、不含句号；
  - 章：`第X章|回|节`（阿拉伯数字序号）或 `第X章|回`（中文数字序号，要求分隔符），长度 ≤ 60；
  - 简介/序类：由前缀匹配改为整行匹配（`内容简介`、`引子`、`序章`、`楔子`、`后记` 等）。
- 过滤残留的 HTML 实体水印行（如 `&#116;…` 拼出的网址）。

### 变更

- 新增 `epub_preprocess.py`：编码探测与文本预处理抽成独立模块（仅依赖标准库），
  提供命令行入口，便于在无 GUI 环境下预处理与自查。
- 新增 `tests/test_preprocess.py`：stdlib unittest，覆盖本次误判样本、常见干扰样本与真实书源回归。
- GUI 预处理完成后在状态栏显示识别到的卷/章/简介数量与过滤的广告行数。
- 已经带 `#` / `##` 前缀的行继续原样透传，可作为手工标记特殊标题的逃生通道。

## [1.0.1] - 2025-03-09

### 修改

- 更改了 UI 布局。
- 增加文本文件预处理，包括转换文件编码为UTF-8, 自动分卷，章节，自动移除广告内容。
- 新增 compile.sh 处理 Linux 环境编译为可执行文件
- 改进卷标题识别规则，现在支持以下格式：
  - "第X卷" 格式（如"第一卷"、"第二卷"）
  - "卷X" 格式（如"卷一"、"卷二"）
